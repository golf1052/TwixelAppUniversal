<Page
    x:Class="TwixelAppUniversal.AppShell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TwixelAppUniversal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SplitView x:Name="RootSplitView" IsPaneOpen="True" DisplayMode="CompactOverlay">
            <SplitView.Pane>
                <StackPanel>
                    <Button Content=" "/>
                    <Button Content="test 1"/>
                    <Button Content="test 2"/>
                </StackPanel>
            </SplitView.Pane>
            <Frame x:Name="frame">
                
            </Frame>
        </SplitView>
        <ToggleButton x:Name="togglePaneButton" VerticalAlignment="Top">
            <SymbolIcon Symbol="More"/>
        </ToggleButton>

    </Grid>
</Page>
